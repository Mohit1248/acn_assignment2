# build artifacts
/server
/client
*.o
*.obj

# generated at runtime - regenerate via scripts; final experiment CSVs/logs
# are committed explicitly as deliverables, not picked up implicitly
/metrics.csv
/health.log

# submission zips
*.zip

# python
__pycache__/
*.pyc

# experiment output (regenerate via scripts/run_experiments.py; commit the
# final CSVs/logs explicitly as deliverables when ready, not implicitly)
/results/

# editor/OS cruft
.vscode/
.DS_Store
